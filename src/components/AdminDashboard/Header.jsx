import { Link } from "react-router-dom";

const Header = () => {
  return (
    <div className="flex justify-between">
      <div className="text-2xl font-medium">Admin Dashboard</div>
    </div>
  );
};

export default Header;
